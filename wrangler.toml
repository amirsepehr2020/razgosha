name = "razgosha"
main = "src/index.js"
compatibility_date = "2026-09-14"

# The D1 binding is intentionally configured in the Cloudflare dashboard
# so the existing database `my-database-bot` can be attached without
# committing its database ID to this public repository.
